# Secrets — never commit
.env
*.env
!.env.example

# Bot-private data — msg content, strategy docs, images, DB (all in database/)
database/

# Local state — DB + ALL user images/data (post records, live images)
database/

# Python
__pycache__/
*.pyc
*.egg-info/

# Project venv (self-contained — never commit)
.venv/
venv/

# Local database
*.db

# Local secrets folder (tokens, app credentials)
.secrets/

# Tests cache
.pytest_cache/

# OS junk
Thumbs.db
.DS_Store
