# Copy this file to .env and fill in your values.
# NEVER commit .env — it contains page/token configuration.

# Your Facebook Page ID (find in Page → About → Page ID, or via /me/accounts)
PAGE_ID=your_page_id_here

# Optional: page name (display only)
PAGE_NAME=optional_page_name
